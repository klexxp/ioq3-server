Place a small tileable PNG named `tile.png` here to use as the landing page background.

Recommended:
- Size: 32x32 or 64x64
- Optimized for web (PNG-8 or PNG-24)
- Repeat-friendly: no seams when tiled

After adding `tile.png`, restart the landing container or reload the page to see the tiled background.