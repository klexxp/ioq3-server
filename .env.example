# Environment variables for ioq3-server stack
# Copy this file to .env and customize for your setup

# Domain for Let's Encrypt SSL (required for production HTTPS)
# Must be a fully qualified domain name pointing to your server's public IP
# For local development, use localhost (will generate self-signed cert)
DOMAIN=localhost

# FastDL subdomain (optional - if set, fastdl will be served on this subdomain)
# Examples: cdn.quake.example.com or dl.example.com
# Leave empty to use path-based access (domain.com/fastdl)
FASTDL_SUBDOMAIN=

# Fast download URL for custom maps
# Path-based: https://quake.example.com/fastdl
# Subdomain: https://cdn.quake.example.com
# Local dev: http://localhost/fastdl
FASTDL_URL=http://localhost/fastdl

# Admin password (leave empty for auto-generated)
ADMIN_PASSWORD=

# Server MOTD
SERVER_MOTD=Welcome to my Quake 3 server!

# Server config files to load (optional overrides)
FFA_CONFIG=server-ffa.cfg
CTF_CONFIG=server-ctf.cfg
Q3TA_CONFIG=server-mp.cfg

# Map rotation config files (optional overrides)
MAPS_FFA=maps-ffa.cfg
MAPS_CTF=maps-ctf.cfg
MAPS_Q3TA=maps-q3ta.cfg

